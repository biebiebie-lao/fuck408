# Numbers and Calculations

## 数据的表示

数据在机器中怎么以二进制进行表示？

### 数的编码

#### 机器数与真值

真值：在现实世界中有正负的值
机器数：计算机内部编码表示后的数

#### 编码

原码： 将正负号用0/1表示，数值部分用二进制编码
反码： 除符号位外，所有位取反
补码： two's complement 和该数互补的值
移码： num + bias 通过加上一个偏置量，迁移数的范围

#### 定点数的表示

**定点数**是小数点位置确定的数。定点数一般用来表示整数

1. 无符号数： 使用二进制原码表示
2. 有符号数： 使用二进制补码表示

#### 浮点数的表示

**浮点数**是小数点浮动的数。常用来表示小数

浮点数的表示由3部分组成：
- 符号位
- 阶码
- 尾数

IEEE754标准下，32位浮点数有1位符号位，8位阶码，23位尾数；64位浮点数有1位符号位，11位阶码，52位尾数

表示范围（阶码用移码表示，尾数规格化后隐含最高位1）：

| 格式 | 阶码偏置值 | 阶码真值范围 | 正数的表示范围 | 最小正数（非规格化） |
| --- | --- | --- | --- | --- |
| 32位单精度 | 127 | -126 ~ +127 | 2^-126 ~ (2 - 2^-23) × 2^127 ≈ 1.18 × 10^-38 ~ 3.40 × 10^38 | 2^-149 ≈ 1.40 × 10^-45 |
| 64位双精度 | 1023 | -1022 ~ +1023 | 2^-1022 ~ (2 - 2^-52) × 2^1023 ≈ 2.23 × 10^-308 ~ 1.80 × 10^308 | 2^-1074 ≈ 4.94 × 10^-324 |

特殊的浮点数（以32位单精度为例，64位只是阶码位数和偏置值不同）：

| 阶码E | 尾数M | 类型 | 真值形式 | 说明 |
| --- | --- | --- | --- | --- |
| 全0 | 全0 | ±0 | +0 / -0 | 符号位不同，但比较时相等 |
| 全0 | 非0 | 非规格化数 | (-1)^S × 0.M × 2^-126 | 阶码按 -126 计算（不是 -127），尾数不隐含1；用于表示比规格化最小数更小的值，且逐步下溢到0时精度损失更平滑 |
| 1 ~ 254 | 任意 | 规格化数 | (-1)^S × 1.M × 2^(E-127) | 常规浮点数都在这里 |
| 全1(255) | 全0 | ±∞ | +∞ / -∞ | 由非0数除以0、或结果上溢产生 |
| 全1(255) | 非0 | NaN | NaN | 非法运算，如 0/0、∞-∞、√-1；尾数非0用来区分具体是哪种NaN |

### 非数值型数据

1. 逻辑值
2. 字符（西文字符，中文字符

## 数据的运算

### 加减运算